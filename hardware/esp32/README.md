# ESP32 Node

This example is the sensor-side extension of the 35% software prototype.

Install Arduino libraries:
- PubSubClient

Wire the PIR sensor signal to GPIO 27. Replace Wi-Fi and MQTT placeholders.

For the university demo, the PC/backend simulation can be used without hardware. The ESP32 sketch is provided to prove the IoT extension path.
